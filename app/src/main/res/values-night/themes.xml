<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Zero Override Policy: Inherit everything from main themes.xml -->
    <style name="Theme.BookUp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Re-applying core colors to ensure absolute dominance -->
        <item name="colorPrimary">@color/md_theme_primary</item>
        <item name="android:colorBackground">@color/md_theme_background</item>
        <item name="colorSurface">@color/md_theme_surface</item>
        <item name="android:statusBarColor">@color/md_theme_background</item>
        <item name="android:navigationBarColor">@color/md_theme_background</item>
    </style>
</resources>
